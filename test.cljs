(ns hello-world.core
  (:require [cljs.core :as cljs]))

(cljs/println "Hello, ClojureScript!")
(ns hello-world.core
  (:require [cljs.core :as cljs]))

(cljs/println "Hello, ClojureScript!")
(ns hello-world.core
  (:require [cljs.core :as cljs]))

(cljs/println "Hello, ClojureScript!")
(ns hello-world.core
  (:require [cljs.core :as cljs]))

(cljs/println "Hello, ClojureScript!")
(ns hello-world.core
  (:require [cljs.core :as cljs]))

(cljs/println "Hello, ClojureScript!")
